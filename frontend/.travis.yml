language: node_js
node_js: 
 - 'stable'
sudo: required
services:
  - docker

before_install:
  - docker build -t bamas/docker-react/frontend -f Dockerfile.dev .

script:
 - docker run -e CI=true bamas/docker-react/frontend npm run test
